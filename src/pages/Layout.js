import Navbar from "../layout/Navbar";
import AppRoutes from "../route/AppRoute";

const Layout = () => {
    return (
        <div>
            <Navbar/>
            <AppRoutes/>
        </div>
    )
};

export default Layout